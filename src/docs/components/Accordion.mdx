import { TabProvider, TabTrigger, TabContent, TabList } from "@pswui/Tabs";
import { Story } from "@/components/Story";
import {
  LoadedCode,
  GITHUB_DIR_COMP,
  GITHUB_STORY,
} from "@/components/LoadedCode";
import Examples from "./AccordionBlocks/Examples";
import Playground from "./AccordionBlocks/Playground";

# Accordion

Displays a set of stacked headings that reveal one section of content at a
time.

## Playground

<Playground />

## Installation

1. Create a new directory named `Accordion` in your component folder.
2. Create following files in the folder, and paste the code into the file.

- `index.ts`
  <LoadedCode from={GITHUB_DIR_COMP("Accordion", "index.ts")} />
- `Context.ts`
  <LoadedCode from={GITHUB_DIR_COMP("Accordion", "Context.ts")} />
- `Component.tsx`
  <LoadedCode from={GITHUB_DIR_COMP("Accordion", "Component.tsx")} />

## Usage

```tsx
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@components/Accordion";
```

```tsx
<Accordion defaultValue="shipping">
  <AccordionItem value="shipping">
    <AccordionTrigger>Shipping</AccordionTrigger>
    <AccordionContent>
      Orders placed before 2pm ship the same day.
    </AccordionContent>
  </AccordionItem>
  <AccordionItem value="item-1">
    <AccordionTrigger>Item 1</AccordionTrigger>
    <AccordionContent>Content for item 1.</AccordionContent>
  </AccordionItem>
</Accordion>
```

## Props

### Accordion

| Prop            | Type                              | Default | Description                                   |
| :-------------- | :-------------------------------- | :------ | :-------------------------------------------- |
| `defaultValue`  | `string`                          | -       | The value of the item that should start open. |
| `value`         | `string \| null`                  | -       | Controls the open item externally.            |
| `onValueChange` | `(value: string \| null) => void` | -       | Called whenever the open item changes.        |
| `collapsible`   | `boolean`                         | `false` | Lets the current item close back to `null`.   |

### AccordionItem

| Prop       | Type      | Default | Description                          |
| :--------- | :-------- | :------ | :----------------------------------- |
| `value`    | `string`  | -       | Unique value used to identify the item. |
| `disabled` | `boolean` | `false` | Prevents the item from being toggled. |

### AccordionTrigger

Accepts standard button props.

### AccordionContent

Accepts standard div props.

## Examples

### Collapsible

<TabProvider defaultName="preview">
  <TabList>
    <TabTrigger name="preview">Preview</TabTrigger>
    <TabTrigger name="code">Code</TabTrigger>
  </TabList>
  <TabContent name="preview">
    <Story layout="centered">
      <Examples.Collapsible />
    </Story>
  </TabContent>
  <TabContent name="code">
    <LoadedCode from={GITHUB_STORY("Accordion", "Collapsible")} />
  </TabContent>
</TabProvider>

### Disabled Item

<TabProvider defaultName="preview">
  <TabList>
    <TabTrigger name="preview">Preview</TabTrigger>
    <TabTrigger name="code">Code</TabTrigger>
  </TabList>
  <TabContent name="preview">
    <Story layout="centered">
      <Examples.DisabledItem />
    </Story>
  </TabContent>
  <TabContent name="code">
    <LoadedCode from={GITHUB_STORY("Accordion", "DisabledItem")} />
  </TabContent>
</TabProvider>
